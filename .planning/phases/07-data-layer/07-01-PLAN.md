---
phase: 07-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/alembic/versions/002_add_request_hash.py]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
user_setup: []
must_haves:
  truths:
    - "Evaluations table has request_hash column"
    - "Composite index exists on (request_hash, status)"
    - "Migration can be reverted without data loss"
  artifacts:
    - path: "backend/alembic/versions/002_add_request_hash.py"
      provides: "Migration adding request_hash column and composite index"
      exports: ["upgrade", "downgrade"]
  key_links:
    - from: "backend/alembic/versions/002_add_request_hash.py"
      to: "evaluations table"
      via: "op.add_column, op.create_index"
      pattern: "add_column.*request_hash.*create_index.*hash_status"
---

<objective>
Add request_hash column and composite index for deduplication

Purpose: Enable efficient deduplication of evaluation requests by tracking request hash
Output: New Alembic migration file that adds column and index with proper downgrade
</objective>

<context>
@backend/app/models.py
@backend/alembic/versions/001_initial.py
@.planning/phases/07-data-layer/07-CONTEXT.md

## Key Definitions from models.py

```python
# Current model already defines these (they just need to be applied via migration):
request_hash = Column(String(64), nullable=True, index=True)
__table_args__ = (Index("ix_evaluations_hash_status", "request_hash", "status"),)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Alembic migration</name>
  <files>backend/alembic/versions/002_add_request_hash.py</files>
  <action>
Generate Alembic migration using autogenerate:
1. Run: `cd backend && uv run alembic revision --autogenerate -m "add request_hash column and index"`
2. Verify the generated migration includes:
   - Adding request_hash column (String(64), nullable)
   - Adding composite index ix_evaluations_hash_status on (request_hash, status)
3. If autogenerate doesn't detect changes, manually create migration with:
   - upgrade(): op.add_column('evaluations', sa.Column('request_hash', sa.String(64), nullable=True))
   - upgrade(): op.create_index('ix_evaluations_hash_status', 'evaluations', ['request_hash', 'status'])
   - downgrade(): op.drop_index('ix_evaluations_hash_status', table_name='evaluations')
   - downgrade(): op.drop_column('evaluations', 'request_hash')
</action>
  <verify>
    <automated>ls backend/alembic/versions/002_add_request_hash.py</automated>
  </verify>
  <done>Migration file exists with upgrade() and downgrade() functions</done>
</task>

<task type="auto">
  <name>Task 2: Verify migration applies cleanly</name>
  <files>backend/alembic/versions/002_add_request_hash.py</files>
  <action>
1. Run: `cd backend && uv run alembic upgrade head`
2. Verify column exists: `psql -c "SELECT request_hash FROM evaluations LIMIT 1" -d resume_screener`
3. Verify index exists: `psql -c "SELECT indexname FROM pg_indexes WHERE indexname = 'ix_evaluations_hash_status'" -d resume_screener`
</action>
  <verify>
    <automated>cd backend && uv run alembic upgrade head 2>&1</automated>
  </verify>
  <done>Migration runs without error, column and index are created in database</done>
</task>

<task type="auto">
  <name>Task 3: Verify downgrade works</name>
  <files>backend/alembic/versions/002_add_request_hash.py</files>
  <action>
1. Run: `cd backend && uv run alembic downgrade -1`
2. Verify column removed: `psql -c "SELECT request_hash FROM evaluations LIMIT 1" -d resume_screener 2>&1` (should error or show no such column)
3. Verify index removed: `psql -c "SELECT indexname FROM pg_indexes WHERE indexname = 'ix_evaluations_hash_status'" -d resume_screener` (should be empty)
4. Re-run upgrade to leave DB in correct state: `cd backend && uv run alembic upgrade head`
</action>
  <verify>
    <automated>cd backend && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <done>Downgrade executes successfully, re-upgrade also works</done>
</task>

</tasks>

<verification>
- DATA-01: request_hash column exists in evaluations table ✓
- DATA-02: Composite index ix_evaluations_hash_status exists ✓
- DATA-03: alembic downgrade -1 executes without error ✓
</verification>

<success_criteria>
Migration file committed with both upgrade and downgrade functions. Database has request_hash column and composite index after upgrade. Downgrade can be executed and re-upgraded without data loss.
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-layer/07-01-SUMMARY.md`
</output>
