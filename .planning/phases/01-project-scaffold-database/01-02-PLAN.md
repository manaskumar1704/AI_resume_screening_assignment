---
phase: 01-project-scaffold-database
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/app/database.py
  - backend/app/models.py
autonomous: true
requirements: []
gap_closure: false
---

# Plan 02: Database Layer + Models

## Objective
Create async SQLAlchemy engine with proper pool settings and the Evaluation ORM model.

## Tasks

### Task 1: Create backend/app/database.py
- **Files:** backend/app/database.py
- **Action:** Create async engine with create_async_engine(). Use pool_size=5, max_overflow=10, pool_recycle=1800. Create async session factory with async_sessionmaker. Make pool settings modular via config. Use asyncpg driver.
- **Verify:** `python -c "from app.database import engine, async_session_maker; print('database OK')"`
- **Done:** Engine connects to PostgreSQL, session maker ready

### Task 2: Create backend/app/models.py
- **Files:** backend/app/models.py
- **Action:** Create Base: declarative_base(). Create Evaluation table with columns: id (UUID, primary key), status (VARCHAR(20), default 'pending'), resume_filename (TEXT), jd_text (TEXT), score (INTEGER, nullable), verdict (VARCHAR(50), nullable), missing_requirements (JSONB, nullable), justification (TEXT, nullable), confidence (FLOAT, nullable), match_percentages (JSONB, nullable), extracted_skills (JSONB, nullable), error_message (TEXT, nullable), created_at (TIMESTAMPTZ, default NOW()), updated_at (TIMESTAMPTZ, default NOW()).
- **Verify:** `python -c "from app.models import Evaluation; print('models OK')"`
- **Done:** Evaluation table defined

## Self-Check
- [ ] Async engine created with proper pool settings
- [ ] Session factory available
- [ ] Evaluation model has all required columns
- [ ] Model imports without errors