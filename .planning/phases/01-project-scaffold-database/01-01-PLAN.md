---
phase: 01-project-scaffold-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - backend/pyproject.toml
  - .env.example
autonomous: true
requirements: []
gap_closure: false
---

# Plan 01: Docker Compose + Python Dependencies + Environment Template

## Objective
Create foundational infrastructure files: docker-compose.yml with PostgreSQL, Redis, and API service, backend/pyproject.toml with all Python dependencies, and .env.example with required environment variables.

## Tasks

### Task 1: Create docker-compose.yml
- **Files:** docker-compose.yml
- **Action:** Create postgres:17 service with healthcheck (pg_isready), redis:7-alpine service with healthcheck, api service built from ./backend context. Volume mounts for persistent data. Service names: postgres, redis, api. Healthchecks on all services. Persistent volumes.
- **Verify:** `docker-compose config` validates successfully
- **Done:** All three files created with correct structure

### Task 2: Create backend/pyproject.toml
- **Files:** backend/pyproject.toml
- **Action:** Create with Python 3.13. Dependencies: fastapi[standard], sqlalchemy[asyncio]=2.0.*, asyncpg, alembic, pydantic-settings, langchain-core, langchain, arq, pdfplumber, tenacity, python-multipart. Use uv for dependency management.
- **Verify:** File created, uv can read it
- **Done:** pyproject.toml at backend/ root

### Task 3: Create .env.example
- **Files:** .env.example
- **Action:** Include LLM settings: LLM_PROVIDER, LLM_MODEL, OPENAI_API_KEY, ANTHROPIC_API_KEY, GROQ_API_KEY. Database: DATABASE_URL. Redis: REDIS_URL. App: DEBUG.
- **Verify:** File created with all env vars
- **Done:** .env.example documents all required environment variables

## Self-Check
- [ ] docker-compose.yml passes config validation
- [ ] pyproject.toml has all required dependencies
- [ ] .env.example contains all necessary environment variables